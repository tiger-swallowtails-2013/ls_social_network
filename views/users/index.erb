<h1>Welcome to my blog!</h1>
<ul>
<% @users.each do |post| %>
  <li>
    <h2><a href="/users/<%= @user.id %>"><%= user.name %></a>
    <span><%= pretty_date(post.created_at) %></span>
    </h2>
  </li>
<% end %>
</ul> 